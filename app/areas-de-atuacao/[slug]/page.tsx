import { notFound } from 'next/navigation';import Link from 'next/link';import { ArrowRight, Check } from 'lucide-react';import { areas, articles } from '@/lib/data';import { PageHero } from '@/components/PageHero';import { CTA } from '@/components/CTA';
export function generateStaticParams(){return areas.map(a=>({slug:a.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=areas.find(x=>x.slug===slug);return {title:a?.title||'Área de atuação',description:a?.summary}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const area=areas.find(x=>x.slug===slug);if(!area)notFound();const related=articles.filter(a=>a.category.toLowerCase().includes(area.title.split(' ')[1]?.toLowerCase()||''));return <><PageHero eyebrow="ÁREA DE ATUAÇÃO" title={area.title} text={area.intro}/><section className="section"><div className="container two-col"><div><h2>Como podemos atuar</h2><p>Entre outras frentes, a atuação pode envolver:</p></div><div className="check-list">{area.services.map(s=><div key={s}><Check size={17}/><span>{s}</span></div>)}</div></div></section><section className="section surface"><div className="container narrow"><span className="eyebrow">NOSSA ABORDAGEM</span><h2>Análise jurídica conectada ao contexto.</h2><p className="lead">{area.approach}</p></div></section>{related.length>0&&<section className="section"><div className="container"><h2>Conteúdo relacionado</h2><div className="article-grid">{related.map(a=><Link className="article-card" href={`/conteudo/${a.slug}`} key={a.slug}><span>{a.category}</span><h3>{a.title}</h3><p>{a.excerpt}</p><span className="text-link">Ler análise <ArrowRight size={15}/></span></Link>)}</div></div></section>}<CTA/></>}
