import Link from 'next/link';import { ArrowRight } from 'lucide-react';import { articles } from '@/lib/data';import { PageHero } from '@/components/PageHero';
export const metadata={title:'Conteúdo'};
export default function Page(){const cats=[...new Set(articles.map(a=>a.category))];return <><PageHero eyebrow="CONHECIMENTO" title="Conteúdo jurídico para decisões mais informadas." text="Artigos e análises sobre temas relevantes do ambiente jurídico e empresarial."/><section className="section"><div className="container"><div className="filter-row"><span>Todos</span>{cats.map(c=><span key={c}>{c}</span>)}</div><div className="article-grid">{articles.map(a=><Link className="article-card" href={`/conteudo/${a.slug}`} key={a.slug}><span>{a.category}</span><h2>{a.title}</h2><p>{a.excerpt}</p><small>{a.date} · {a.readTime}</small><span className="text-link">Ler artigo <ArrowRight size={15}/></span></Link>)}</div></div></section></>}
