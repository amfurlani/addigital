import Link from 'next/link';import { ArrowRight } from 'lucide-react';import { lawyers } from '@/lib/data';import { PageHero } from '@/components/PageHero';import { CTA } from '@/components/CTA';
export const metadata={title:'Equipe'};
export default function Page(){return <><PageHero eyebrow="EQUIPE" title="Pessoas por trás da estratégia jurídica." text="Profissionais com diferentes experiências e formações, atuando de maneira integrada."/><section className="section"><div className="container team-grid">{lawyers.map(p=><Link className="person-card large" href={`/equipe/${p.slug}`} key={p.slug}><div className="avatar">{p.name.split(' ').slice(0,2).map(x=>x[0]).join('')}</div><span>{p.role}</span><h2>{p.name}</h2><small>{p.oab}</small><p>{p.areas.join(' · ')}</p><span className="text-link">Conheça o perfil <ArrowRight size={15}/></span></Link>)}</div></section><CTA/></>}
